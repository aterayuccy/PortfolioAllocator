# PostgreSQL container settings used by compose.yaml
POSTGRES_DB=portfolio_allocator
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=portfolio
POSTGRES_PORT=5432

# Spring Boot settings (use the same credentials as the container)
DB_URL=jdbc:postgresql://localhost:5432/portfolio_allocator
DB_USERNAME=portfolio
DB_PASSWORD=portfolio
